homeassistant==2026.9.1
pytest-homeassistant-custom-component==0.13.364
pytest-cov
ruff
mypy
# Cast Notifier depends on the `tts` integration, which in turn depends on
# `ffmpeg`/`mutagen` at setup time; these are not installed automatically
# by pytest-homeassistant-custom-component. Versions pinned to what
# homeassistant==2026.9.1's tts/ffmpeg manifests require.
ha-ffmpeg==3.2.2
mutagen==1.48.1
